import { PresupuestoPipe } from './presupuesto.pipe';

describe('PresupuestoPipe', () => {
  it('create an instance', () => {
    const pipe = new PresupuestoPipe();
    expect(pipe).toBeTruthy();
  });
});
